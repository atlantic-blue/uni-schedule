# Copy to .env.local and fill in. With these empty the app runs on demo data.
# Create the project in an European Union region, because the data includes
# absences.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
